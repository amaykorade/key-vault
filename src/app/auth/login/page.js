import LoginForm from '../../../components/forms/LoginForm'

export const metadata = {
  title: 'Sign In - Key Vault',
  description: 'Sign in to your Key Vault account to access your secure keys and passwords.',
}

export default function LoginPage() {
  return <LoginForm />
} 